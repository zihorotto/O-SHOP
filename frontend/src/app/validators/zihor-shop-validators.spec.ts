import { ZihorShopValidators } from './zihor-shop-validators';

describe('ZihorShopValidators', () => {
  it('should create an instance', () => {
    expect(new ZihorShopValidators()).toBeTruthy();
  });
});
